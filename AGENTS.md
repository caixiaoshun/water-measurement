# AGENTS.md

## 项目背景
本项目面向河道、渠道、水库等水域的边坡/水工场景监测，采用单目相机（普通监控枪机/球机）实现非接触式、可视化的距离测量。典型使用方式为：用户在视频画面中选择任意两点，系统输出两点在水面参考坐标系下的实际距离，用于替代或补充人工丈量，提升巡检与工程监测效率。

实际部署环境具有显著动态性与复杂性：水位会随时间变化；相机可能因安装结构、风载、维护等因素产生姿态或位置漂移；户外光照、天气、水面波纹/反光、遮挡与背景纹理等都会影响视觉测量的稳定性与可靠性。因此需要将传统单目几何测距能力扩展为“能适应环境变化、可工程化部署”的系统。

## 项目目标（以合同考核为准）
交付一套“面向动态水面场景的自适应单目相机视觉测距系统”，达到以下指标：

1. 测距误差：在标准测试条件下（相机距水面约 2–50 米），对任意两点距离测量结果的相对误差稳定控制在 5% 以内。  
2. 外参自动修正成功率：当相机姿态发生变化（平移 > 5 cm 或倾斜 > 2°）时，系统可基于基准点自动完成外参修正，成功率不低于 95%。  
3. 修正后精度恢复：完成自动修正后，测距精度恢复至初始标定精度的 90% 以上。  
4. 响应时间：从用户点选两点到输出距离结果的系统响应时间不超过 1 秒。  
5. 标定通用性：系统应能适配并完成至少 3 种不同品牌/型号（含枪机、球机）相机的内参标定或参数接入。  
6. 基准点兼容性：系统至少支持 2 种以上不同类型基准点（如 ArUco 码、圆形标志、自定义图案）的识别与定位。  
7. 输出成果：交付技术报告及全部源代码。